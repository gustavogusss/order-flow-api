spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/order_flow
    username: order_user
    password: order_pass

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate